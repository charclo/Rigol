.DS_Store

[Bb]in/
[Oo]bj/

# Generated by the mask bench. Commit a mask.json deliberately if it is a production mask.
*.envelope.csv
waveform_data.csv

# Transfer files from scripts/site-sync.sh (commits made on a disconnected
# site, carried back on a USB stick/shared drive). Never commit the bundle
# itself -- apply it with scripts/apply-bundle.sh and push the real commits.
/site-bundles/
